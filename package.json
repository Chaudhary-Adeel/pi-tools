{
  "name": "pi-coding-toolkit",
  "version": "0.1.0",
  "description": "A batteries-included Pi package: web fetch/search, file I/O, shell, code search, ask-user, and parallel subagent spawning, plus a token-efficient system prompt.",
  "keywords": [
    "pi-package"
  ],
  "type": "module",
  "license": "MIT",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "scripts": {
    "install:pi": "bash install.sh",
    "reinstall": "bash install.sh"
  },
  "devDependencies": {
    "@types/node": "^25.9.2"
  }
}
